apiVersion: apps/v1
kind: Deployment
metadata:
  name: toto
  labels:
    app: label2
spec:
  replicas: 5
  selector:
    matchLabels:
      app: label2
  template:
    metadata:
      labels:
        app: label2
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
